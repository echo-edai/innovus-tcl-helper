{
    "command": "append",
    "category": "Core",
    "summary": "Append string to a variable",
    "usage": "append varName ?value value ...?",
    "description": "Append each value to the current value of varName. If varName does not exist, creates it with value. Returns the new value. More efficient than set varName $varName$value."
}

{
  "command": "foreach",
  "category": "Control Flow",
  "summary": "Iterate over list elements",
  "usage": "foreach varname list body\nforeach {var1 var2 ...} list body\nforeach varlist1 list1 ?varlist2 list2 ...? body",
  "description": "Iterate over list elements. Each iteration assigns the next element from list to varname, then executes body. Multi-variable form {var1 var2} takes multiple elements per iteration. Also supports parallel iteration over multiple lists."
}

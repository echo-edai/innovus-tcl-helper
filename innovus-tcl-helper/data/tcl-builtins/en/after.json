{"command":"after","category":"Time","summary":"Execute script after a delay or periodically","usage":"after ms ?script script ...?","description":"Execute script after ms milliseconds. Without script, returns a cancellable ID. after cancel id cancels. after idle script executes when event loop is idle.","note":"Commonly used for delayed operations and periodic polling."}

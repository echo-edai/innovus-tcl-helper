{
  "command": "switch",
  "category": "Control Flow",
  "summary": "Multi-way branch statement",
  "usage": "switch ?options? string pattern body ?pattern body ...?\nswitch ?options? string {pattern body ?pattern body ...?}",
  "description": "Match string against each pattern in order, executing the body of the first match. Supports -exact, -glob, -regexp matching modes. default acts as a catch-all. Patterns can share a body using -."
}

{
    "command": "incr",
    "category": "Core",
    "summary": "Increment a variable by an integer",
    "usage": "incr varName ?increment?",
    "description": "Increment the value of varName by increment (default 1). The variable must hold an integer. Returns the new value. Commonly used as a counter."
}

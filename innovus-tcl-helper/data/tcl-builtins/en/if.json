{
  "command": "if",
  "category": "Control Flow",
  "summary": "Conditional branch statement",
  "usage": "if expr1 ?then? body1 ?elseif expr2 ?then? body2 ...? ?else? ?bodyN?",
  "description": "If expr1 is true (non-zero/true/yes), execute body1. Otherwise check each elseif condition in order. Execute else bodyN if none match. then and else are optional readability keywords. Returns the result of the executed body."
}

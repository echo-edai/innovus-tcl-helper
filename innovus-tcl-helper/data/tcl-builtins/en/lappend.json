{"command":"lappend","category":"List","summary":"Append elements to a list variable","usage":"lappend varName ?value value ...?","description":"Append each value as a new element to the end of the list in varName. Creates a new list if varName does not exist. Returns the new list value. Unlike append, preserves list structure.","note":"Unlike append: lappend preserves TCL list structure; append is plain string concatenation."}

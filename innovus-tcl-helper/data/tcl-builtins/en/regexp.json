{"command":"regexp","category":"String","summary":"Regular expression matching","usage":"regexp ?switches? exp string ?matchVar? ?subMatchVar ...?","description":"Determine if regex exp matches string; returns 1 or 0. Optionally stores overall match and subexpression matches in variables. Supports -nocase, -all, -inline, -indices, -start, -line switches."}

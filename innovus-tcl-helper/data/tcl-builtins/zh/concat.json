{"command":"concat","category":"字符串","summary":"连接多个列表","usage":"concat ?arg arg ...?","description":"将多个参数连接成一个列表。与 list 不同的是，concat 会将每个参数的顶层列表元素合并（去一层嵌套），而 list 保持嵌套不变。","note":"concat {a b} {c d} → a b c d；list {a b} {c d} → {a b} {c d}"}

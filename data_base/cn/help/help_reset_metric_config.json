{
  "command": "reset_metric_config",
  "is_cmd": true,
  "summary": "将指标页面配置重置为默认设置",
  "description": "此命令用于将报告指标的页面配置重置为系统默认状态。",
  "usage": "reset_metric_config [-help] [-format {html excel text}]",
  "options": [
    {
      "name": "-help",
      "description": "打印出此命令的使用说明",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-format",
      "description": "为指定的报告指标格式进行配置（可选值为 html、excel、text）",
      "required": false,
      "type": "enum"
    }
  ]
}
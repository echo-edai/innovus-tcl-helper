{
  "command": "changeCurrentDesign",
  "is_cmd": true,
  "summary": "更改当前顶层设计或分区",
  "description": "此命令用于将指定的设计单元或分区设置为当前工作的顶层设计。这在处理分层设计或需要切换当前焦点到不同分区时非常有用。",
  "usage": "changeCurrentDesign [-help] [<ptnName <partition name>>]",
  "options": [
    {
      "name": "-help",
      "description": "打印此命令的使用说明",
      "required": false,
      "type": "flag"
    },
    {
      "name": "<ptnName <partition name>>",
      "description": "指定要设置为顶层设计的分区名称",
      "required": false,
      "type": "string"
    }
  ]
}
{
  "command": "uniquify_partition",
  "is_cmd": true,
  "summary": "对指定的层次化实例进行唯一化处理",
  "description": "此命令用于对设计中指定的层次化模块实例进行唯一化（uniquify）操作。唯一化是物理设计中的一个步骤，它通过为层次化模块的每个实例创建独立的副本，来解决由于实例共享同一模块定义而可能产生的命名冲突或其他问题，确保设计的正确性和可实施性。",
  "usage": "uniquify_partition [-help] -hinst <hierarchical_instance_name>",
  "options": [
    {
      "name": "-help",
      "description": "打印此命令的使用说明",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-hinst",
      "description": "指定要进行唯一化处理的层次化实例的名称",
      "required": true,
      "type": "string"
    }
  ]
}
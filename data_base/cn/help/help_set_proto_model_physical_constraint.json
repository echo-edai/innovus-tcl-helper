{
  "command": "set_proto_model_physical_constraint",
  "is_cmd": true,
  "summary": "设置原型模型的物理约束",
  "description": "此命令用于为原型模型（proto model）设置物理约束类型，例如引导（guide）、围栏（fence）、区域（region）、软引导（soft_guide）或无约束（none）。",
  "usage": "set_proto_model_physical_constraint [-help] [-model <string>] -type {guide fence region soft_guide none}",
  "options": [
    {
      "name": "-help",
      "description": "打印此命令的使用说明",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-model",
      "description": "指定要应用约束的flexmodel列表（字符串，可选）",
      "required": false,
      "type": "string"
    },
    {
      "name": "-type",
      "description": "约束类型（枚举，必需）",
      "required": true,
      "type": "enum"
    }
  ]
}
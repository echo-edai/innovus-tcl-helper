{
  "command": "reset_max_time_borrow",
  "is_cmd": true,
  "summary": "重置对象的最大时间借用限制",
  "description": "该命令用于重置指定对象（如引脚、实例或时钟）的最大时间借用（max_time_borrow）限制。通过提供对象列表，可以清除这些对象上先前设置的时间借用约束，使其恢复默认行为。",
  "usage": "reset_max_time_borrow [-help] <object_list>",
  "options": [
    {
      "name": "-help",
      "description": "打印命令的使用说明",
      "required": false,
      "type": "flag"
    },
    {
      "name": "<object_list>",
      "description": "对象列表，可包含引脚、实例或时钟",
      "required": true,
      "type": "string"
    }
  ]
}
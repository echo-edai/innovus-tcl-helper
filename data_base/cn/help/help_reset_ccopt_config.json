{
  "command": "reset_ccopt_config",
  "is_cmd": true,
  "summary": "重置时钟树优化配置",
  "description": "此命令用于重置当前的时钟树优化（CCopt）配置。它提供了一个选项，允许用户选择是否保留当前在时钟树末端（sink）引脚上配置的插入延迟（insertion delay）。",
  "usage": "reset_ccopt_config [-help] [-preserve_sink_insertion_delays]",
  "options": [
    {
      "name": "-help",
      "description": "打印此命令的使用说明",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-preserve_sink_insertion_delays",
      "description": "是否保留当前在时钟树末端引脚上配置的插入延迟（布尔值，可选）",
      "required": false,
      "type": "flag"
    }
  ]
}
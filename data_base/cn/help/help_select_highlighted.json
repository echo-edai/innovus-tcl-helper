{
  "command": "select_highlighted",
  "is_cmd": true,
  "summary": "选择当前高亮对象",
  "description": "此命令用于选择当前在图形用户界面（GUI）中高亮显示的对象。可以通过 -type 参数指定要选择的对象类型，例如实例（instance）、线网（wire）或通孔（via）。",
  "usage": "select_highlighted [-help] [-type {instance wire via}]",
  "options": [
    {
      "name": "-help",
      "description": "打印此命令的使用说明",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-type",
      "description": "指定要选择的对象类型，可选值为 instance（实例）、wire（线网）或 via（通孔）",
      "required": false,
      "type": "enum"
    }
  ]
}
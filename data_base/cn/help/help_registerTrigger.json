{
  "command": "registerTrigger",
  "is_cmd": true,
  "summary": "注册触发器命令",
  "description": "该命令用于在指定命令执行前或执行后注册一个触发器过程。",
  "usage": "registerTrigger [-help] [{-pre | -post } <commandName> <procName>]",
  "options": [
    {
      "name": "-help",
      "description": "打印命令用法",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-pre",
      "description": "指定为前置触发器（在命令执行前触发）",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-post",
      "description": "指定为后置触发器（在命令执行后触发）",
      "required": false,
      "type": "flag"
    },
    {
      "name": "<commandName>",
      "description": "指定命令名称（字符串，必需）",
      "required": true,
      "type": "string"
    },
    {
      "name": "<procName>",
      "description": "指定过程名称（字符串，必需）",
      "required": true,
      "type": "string"
    }
  ]
}
{
  "command": "register_gui_edit_callback",
  "is_cmd": true,
  "summary": "注册一个GUI编辑操作的回调函数",
  "description": "此命令用于注册一个回调函数，该函数将在图形用户界面（GUI）中发生特定类型的编辑操作（如选择、移动前、移动后）时被触发执行。",
  "usage": "register_gui_edit_callback [-help] -command <string> -edit_type {selection pre-move post-move}",
  "options": [
    {
      "name": "-help",
      "description": "打印此命令的使用说明",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-command",
      "description": "指定要注册的回调函数（字符串形式）",
      "required": true,
      "type": "string"
    },
    {
      "name": "-edit_type",
      "description": "指定触发回调的编辑操作类型（枚举值：选择、移动前、移动后）",
      "required": true,
      "type": "enum"
    }
  ]
}
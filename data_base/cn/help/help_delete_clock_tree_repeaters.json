{
  "command": "delete_clock_tree_repeaters",
  "is_cmd": true,
  "summary": "删除时钟树中继器",
  "description": "该命令用于删除时钟树中的中继器（repeaters）。使用 -force 选项可以强制删除所有中继器，即使它们被标记为固定（fixed）或禁止触碰（dont_touch）。",
  "usage": "delete_clock_tree_repeaters [-help] [-force]",
  "options": [
    {
      "name": "-help",
      "description": "打印此命令的使用说明",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-force",
      "description": "强制删除所有中继器，包括被标记为固定或禁止触碰的",
      "required": false,
      "type": "flag"
    }
  ]
}
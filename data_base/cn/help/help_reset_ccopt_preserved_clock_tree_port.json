{
  "command": "reset_ccopt_preserved_clock_tree_port",
  "is_cmd": true,
  "summary": "重置CCopt（时钟树优化）中受保护的时钟树端口",
  "description": "该命令用于重置在CCopt（时钟树优化）流程中被标记为受保护的特定时钟树端口。执行此命令后，指定的端口将不再被视为受保护，其时钟树结构可能在后续优化中被修改。",
  "usage": "reset_ccopt_preserved_clock_tree_port [-help] {<portname>}",
  "options": [
    {
      "name": "-help",
      "description": "打印此命令的使用说明",
      "required": false,
      "type": "flag"
    },
    {
      "name": "<portname>",
      "description": "需要重置保护状态的端口名称",
      "required": true,
      "type": "string"
    }
  ]
}
{
  "command": "reset_max_time_borrow",
  "is_cmd": true,
  "summary": "重置指定对象的最大时间借用限制",
  "description": "此命令用于重置指定引脚、实例或时钟的最大时间借用限制。",
  "usage": "reset_max_time_borrow <object_list>",
  "options": [
    {
      "name": "<object_list>",
      "description": "指定需要重置时间借用限制的引脚、实例或时钟的列表",
      "required": true,
      "type": "string"
    }
  ]
}
{
  "command": "timing_all_registers_identify_macros_include_is_macro_cell",
  "is_cmd": false,
  "summary": "控制时序分析中是否将具有 is_macro_cell 属性的单元识别为宏单元",
  "description": "此全局变量用于控制 `all_registers` 命令在识别寄存器时，是否将库中标记了 `is_macro_cell` 属性的单元包含为宏单元。当设置为 true（默认值）时，软件会将库中 `is_macro_cell` 属性为 true 的单元视为宏单元并包含在结果中。当设置为 false 时，`all_registers` 命令将不会返回那些 `is_macro_cell` 属性为 true 的单元。需要使用 `set` 命令来设置此变量。",
  "usage": null,
  "options": null
}
{
  "command": "unplaceAllInsts",
  "is_cmd": true,
  "summary": "将所有实例从版图中移除，包括状态为已放置的宏块和标准单元",
  "description": "该命令用于将所有实例从版图中移除，包括状态为PLACED的宏块（blobs）和标准单元（std cells）。执行后，这些实例的状态将从PLACED更改为UNPLACED。此命令可在导入设计后使用。注意：unplaceAllInsts命令不会处理状态为FIXED、SOFTFIXED或COVER的标准单元。",
  "usage": "unplaceAllInsts [-help]",
  "options": [
    {
      "name": "-help",
      "description": "输出unplaceAllInsts命令的简要描述。",
      "required": false,
      "type": "flag"
    }
  ]
}
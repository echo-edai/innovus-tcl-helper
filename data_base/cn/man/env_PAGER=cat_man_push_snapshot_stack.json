{
  "command": "push_snapshot_stack",
  "is_cmd": true,
  "summary": "创建一个用于指标捕获的层级",
  "description": "创建一个用于指标捕获的层级。在该层级内捕获的所有指标将在快照创建（隐式或显式）时保存在此层级。快照层级可以嵌套，因此支持任意数量的层级。注意：每个 push_snapshot_stack 必须有一个对应的 pop_snapshot_stack。",
  "usage": "push_snapshot_stack",
  "options": null
}
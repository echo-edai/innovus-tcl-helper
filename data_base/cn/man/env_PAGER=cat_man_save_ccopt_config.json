{
  "command": "save_ccopt_config",
  "is_cmd": true,
  "summary": "将CCOpt配置写入指定目录",
  "description": "此命令将当前的CCOpt（时钟树优化）配置脚本写入用户指定的目录。注意：生成的文件内容是专有的，可能没有特定格式或不可读，且可能发生变化。这些文件的唯一受支持用途是作为restore_ccopt_config命令的输入。",
  "usage": "save_ccopt_config [-help] <output_path>",
  "options": [
    {
      "name": "-help",
      "description": "输出包含每个参数的简要描述（类型和默认值信息）。",
      "required": false,
      "type": "flag"
    },
    {
      "name": "<output_path>",
      "description": "指定用于保存CCOpt配置脚本的用户自定义目录路径。",
      "required": true,
      "type": "string"
    }
  ]
}
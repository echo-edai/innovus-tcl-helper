{
  "command": "timing_extract_model_aocv_mode",
  "is_cmd": false,
  "summary": "指定AOCV降额模式的全局变量",
  "description": "该变量用于指定在提取时序模型时使用的AOCV降额模式。有效值包括：graph_based（使用基于图的级数进行降额）、path_based（使用最差路径的总路径级数进行降额）以及none（不使用AOCV降额，为基础延迟）。注意：在将此全局变量设置为graph_based或path_based之前，必须先读入AOCV库并启用AOCV分析（通过设置set_analysis_mode -aocv true）。",
  "usage": null,
  "options": null
}
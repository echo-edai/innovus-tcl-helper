{
  "command": "layerNameNoAbbreviation",
  "is_cmd": false,
  "summary": "控制层名返回格式的全局变量",
  "description": "这是一个布尔类型的全局变量，用于控制 dbGet layer.name 的返回值以及大多数命令中 -layer 选项的输入格式。默认情况下（值为0），允许使用 M1、M2 等缩写来表示布线层1、2。当此变量设置为1时，dbGet layer.name 将返回完整的层名（如 Metal1），并且禁止使用 M1 等缩写作为 -layer 的输入，只允许使用完整层名或布线层编号。此设置主要用于处理技术中真实层名（如 M1）与布线层编号（如1）不匹配的情况，以避免混淆。",
  "usage": null,
  "options": null
}
{
  "command": "timing_default_opcond_per_lib",
  "is_cmd": false,
  "summary": "控制未明确指定工作条件时，默认工作条件的选择行为。",
  "description": "该全局变量决定了当用户未通过 setOpcond 命令明确指定工作条件时，默认工作条件的行为方式。当设置为 true 时，每个时序库使用其内部定义的默认工作条件规格。当设置为 false 时，将使用主库（即库搜索列表中的第一个库）的默认工作条件来为整个设计设置 PVT（工艺、电压、温度）工作点，所有时序库都将被访问并根据该工作点进行降额计算。此变量需使用 set_global 命令进行设置。",
  "usage": null,
  "options": null
}
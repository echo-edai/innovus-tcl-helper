{
  "command": "reset_clock_transition",
  "is_cmd": true,
  "summary": "移除指定时钟波形上的时钟转换时间断言",
  "description": "此命令用于移除先前通过 `set_clock_transition` 命令在指定时钟波形列表上设置的时钟转换时间（transition time）断言。时钟转换时间断言会影响静态时序分析（STA）中时钟网络的延迟计算。移除后，相关时钟的转换时间将恢复为默认值或由其他设置决定。",
  "usage": "reset_clock_transition clock_list",
  "options": [
    {
      "name": "clock_list",
      "description": "指定一个时钟波形列表，该列表上的时钟转换时间断言将被移除。此变量可以是一个 Tcl 列表或时钟波形集合。",
      "required": true,
      "type": "clock+"
    }
  ]
}
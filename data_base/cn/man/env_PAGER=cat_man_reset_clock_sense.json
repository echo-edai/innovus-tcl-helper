{
  "command": "reset_clock_sense",
  "is_cmd": true,
  "summary": "重置使用 set_clock_sense 命令指定的时钟感应约束",
  "description": "此命令用于移除先前通过 set_clock_sense 命令设置的时钟感应约束。它可以重置所有约束，或针对特定的时钟和引脚/端口列表进行重置。",
  "usage": "reset_clock_sense [-all] | [[-clocks <clock_list>] <pin_or_port_list>]]",
  "options": [
    {
      "name": "-all",
      "description": "重置所有已指定的时钟感应约束",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-clocks",
      "description": "指定要移除时钟感应约束的时钟列表。这些时钟必须先前已通过 set_clock_sense 命令的 -clock 参数定义。",
      "required": false,
      "type": "string"
    }
  ]
}
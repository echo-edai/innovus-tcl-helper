{
  "command": "select_highlighted",
  "is_cmd": true,
  "summary": "选择高亮对象",
  "description": "此命令用于选择当前高亮的对象。如果指定了 -type 参数，则可以选择指定类型的高亮对象。",
  "usage": "select_highlighted [-help] [-type <string>]",
  "options": [
    {
      "name": "-help",
      "description": "打印包含每个参数类型和默认信息的简要描述。",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-type <string>",
      "description": "指定要选择的高亮对象的类型。可选值为 instance（实例）、wire（线网）和 via（过孔）。",
      "required": false,
      "type": "string"
    }
  ]
}
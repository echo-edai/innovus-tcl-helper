{
  "command": "zoomOut",
  "is_cmd": true,
  "summary": "将可视窗口缩小两倍",
  "description": "此命令用于将当前可视窗口的显示范围放大（即缩小显示比例），使视图内容显示得更小，从而在窗口中看到更大的物理设计区域。每次执行默认将视图缩小为原来的两倍。",
  "usage": "zoomOut [-help]",
  "options": [
    {
      "name": "-help",
      "description": "打印zoomOut命令的简要描述信息。",
      "required": false,
      "type": "flag"
    }
  ]
}
{
  "command": "set_proto_model_physical_constraint",
  "is_cmd": true,
  "summary": "设置FlexModels到特定的物理约束以指导布局",
  "description": "此命令用于将FlexModels设置为特定的物理约束类型，如引导（guide）、围栏（fence）、区域（region）、软引导（soft_guide）或无（none），以指导后续的布局过程。默认情况下，所有FlexModels都将被设置为指定的类型。",
  "usage": "set_proto_model_physical_constraint [-help] -type {guide | fence | region | soft_guide | none} [-model <string>]",
  "options": [
    {
      "name": "-help",
      "description": "打印命令用法信息",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-type",
      "description": "指定约束类型。可选值为：guide, fence, region, soft_guide, none",
      "required": true,
      "type": "enum"
    },
    {
      "name": "-model",
      "description": "指定一个FlexModels列表",
      "required": false,
      "type": "string"
    }
  ]
}
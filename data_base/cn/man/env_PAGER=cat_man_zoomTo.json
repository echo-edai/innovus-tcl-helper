{
  "command": "zoomTo",
  "is_cmd": true,
  "summary": "将可视窗口缩放至指定坐标点，默认半径为150微米",
  "description": "该命令将图形界面的可视窗口中心定位到由坐标(x, y)定义的点，并以该点为中心进行缩放。默认的缩放半径是150微米。用户也可以通过图形界面的选择条(Select bar)直接指定缩放区域的中心和半径。",
  "usage": "zoomTo [-help] x y [-radius <value>]",
  "options": [
    {
      "name": "-help",
      "description": "打印包含每个参数类型和默认信息的简要描述。",
      "required": false,
      "type": "flag"
    },
    {
      "name": "x",
      "description": "缩放中心点的X坐标。",
      "required": true,
      "type": "float"
    },
    {
      "name": "y",
      "description": "缩放中心点的Y坐标。",
      "required": true,
      "type": "float"
    },
    {
      "name": "-radius",
      "description": "指定缩放半径的值（单位：微米），中心点为指定的(x, y)坐标。",
      "required": false,
      "type": "float"
    }
  ]
}
{
  "command": "reset_annotated_transition",
  "is_cmd": true,
  "summary": "重置由set_annotated_transition命令设置的标注转换时间",
  "description": "此命令用于移除先前通过set_annotated_transition命令在指定引脚、端口或整个设计上设置的标注转换时间约束。它不会影响通过set_input_transition命令设置的输入转换时间约束。",
  "usage": "reset_annotated_transition {-all | <port_or_pin_list>}",
  "options": [
    {
      "name": "-all",
      "description": "重置设计中所有已标注的转换时间",
      "required": false,
      "type": "flag"
    },
    {
      "name": "<port_or_pin_list>",
      "description": "指定要重置转换时间的特定引脚或端口的列表",
      "required": false,
      "type": "net+"
    }
  ]
}
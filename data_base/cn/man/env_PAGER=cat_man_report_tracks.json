{
  "command": "report_tracks",
  "is_cmd": true,
  "summary": "报告每个金属层的布线轨道定义",
  "description": "报告每个金属层的布线轨道定义。输出格式为DEF TRACKS格式。该命令可在创建版图规划后或使用add_tracks命令后使用，以查看具体创建了哪些轨道。",
  "usage": "report_tracks [-help] [-prefer_only]",
  "options": [
    {
      "name": "-help",
      "description": "打印简要描述，包括每个report_tracks参数的类型和默认信息。",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-prefer_only",
      "description": "仅报告每个金属层首选方向（preferred direction）的轨道定义。",
      "required": false,
      "type": "flag"
    }
  ]
}
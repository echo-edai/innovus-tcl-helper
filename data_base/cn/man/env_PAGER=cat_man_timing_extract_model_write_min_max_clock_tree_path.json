{
  "command": "timing_extract_model_write_min_max_clock_tree_path",
  "is_cmd": false,
  "summary": "控制是否在时序提取模型中写入每个时钟源的最小/最大最差延迟",
  "description": "这是一个全局变量，用于控制时序提取模型（timing extract model）的写入行为。当设置为 true 时，工具会为每个时钟源写入其最小和最大最差延迟（worst latency）信息。这有助于在后续分析中更全面地评估时钟树路径的时序性能。默认值为 true。要设置此全局变量，需使用 set 命令。",
  "usage": null,
  "options": null
}
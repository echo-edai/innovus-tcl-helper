{
  "command": "reset_load",
  "is_cmd": true,
  "summary": "重置指定网络或端口上已存在的负载设置断言",
  "description": "此命令用于重置在顶层单元的指定网络或端口上已通过set_load命令设置的负载断言。",
  "usage": "reset_load <object_list>",
  "options": [
    {
      "name": "<object_list>",
      "description": "指定一个顶层单元中的网络或端口列表，用于重置其上的负载断言。",
      "required": true,
      "type": "net+"
    }
  ]
}
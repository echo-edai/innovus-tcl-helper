{
  "command": "get_metric_config",
  "is_cmd": true,
  "summary": "获取当前的度量配置设置",
  "description": "此命令用于获取当前度量报告的配置设置，包括定义的页面、标题和表格等信息。可以通过指定输出格式（如html、excel或text）来配置报告。",
  "usage": "get_metric_config -help -format {html excel text}",
  "options": [
    {
      "name": "-help",
      "description": "输出每个get_metric_config参数的简要描述，包括类型和默认信息。",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-format",
      "description": "为指定的report_metric格式指定配置。可选值包括html、excel和text。",
      "required": false,
      "type": "enum"
    }
  ]
}
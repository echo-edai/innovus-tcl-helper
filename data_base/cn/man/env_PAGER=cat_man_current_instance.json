{
  "command": "current_instance",
  "is_cmd": true,
  "summary": "设置当前操作实例",
  "description": "该命令用于将指定的层次化实例设置为当前实例。在此之后执行的命令所引用的设计对象都将相对于此实例进行查找。所有设计对象的搜索都将从此实例开始。如果设计对象是以层次化方式引用的，那么指定的实例将被用作层次结构的根（顶部）。",
  "usage": "current_instance [<hierarchical_instance>]",
  "options": [
    {
      "name": "<hierarchical_instance>",
      "description": "指定要成为当前实例的实例名称。如果未提供此参数，则当前实例将被设置为层次结构的顶层实例。",
      "required": false,
      "type": "inst+"
    }
  ]
}
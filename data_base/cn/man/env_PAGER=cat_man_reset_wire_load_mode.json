{
  "command": "reset_wire_load_mode",
  "is_cmd": true,
  "summary": "重置线负载模式为默认的顶层模式",
  "description": "此命令用于将线负载模式重置为默认的顶层模式。注意：要启用线负载模式，需要设置全局变量 `set soceSupportWireLoadMode 1`。要使用线负载模型功能，必须通过命令 `set_var enable_wire_load_model_support 1` 启用全局变量 `enable_wire_load_model_support`。",
  "usage": "reset_wire_load_mode",
  "options": null
}
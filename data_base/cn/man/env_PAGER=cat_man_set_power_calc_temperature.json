{
  "command": "set_power_calc_temperature",
  "is_cmd": true,
  "summary": "设置静态功耗计算所用的温度",
  "description": "此命令用于设置进行静态功耗计算时的温度。当存在 trilib 库时，内部功耗和泄漏功耗将根据给定的温度规格进行插值计算。注意：静态功耗计算软件会按以下优先级顺序获取每个实例的温度值：1. 实例温度文件；2. set_power_calc_temperature 命令；3. 当前工作条件温度。",
  "usage": "set_power_calc_temperature <temperature>",
  "options": [
    {
      "name": "<temperature>",
      "description": "指定用于静态功耗计算的温度值。",
      "required": true,
      "type": "int"
    }
  ]
}
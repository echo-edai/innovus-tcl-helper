{
  "command": "timing_report_enable_use_valid_start_end_points",
  "is_cmd": false,
  "summary": "控制时序报告中是否显示有效的起点和终点",
  "description": "此变量用于控制在执行`report_timing`命令时，时序报告中是否包含并显示路径上有效的起点和终点。当设置为`true`时，时序报告将包含路径的实际起点和终点引脚（例如，对于寄存器，起点是其CK引脚，终点是其D引脚；对于路径，起点是输入引脚，终点是输出引脚）。当设置为`false`（默认值）时，路径中的任何点都可以被指定为`-from`点，并在内部被视为`-through`点，报告会从指定的`-from`点开始，但路径仍会正确报告到实际的起点和终点。此全局变量需使用`set`命令进行设置。",
  "usage": null,
  "options": null
}
{
  "command": "reset_sdf_assertions",
  "is_cmd": true,
  "summary": "重置SDF延迟值为库中定义的值",
  "description": "此命令用于将SDF（标准延迟格式）延迟重置为库中定义的值。在进行时序分析时，软件默认使用通过read_sdf命令定义的SDF值。此命令可以丢弃通过SDF文件指定的延迟值，转而使用库中定义的延迟。注意：每次执行延迟计算时，无需重新读取SDF延迟，软件会使用流程中先前指定的SDF文件。",
  "usage": "reset_sdf_assertions",
  "options": [
    {
      "name": "-help",
      "description": "打印命令用法信息",
      "required": false,
      "type": "flag"
    }
  ]
}
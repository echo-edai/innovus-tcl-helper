{
  "command": "reset_clock",
  "is_cmd": true,
  "summary": "移除先前创建的时钟声明",
  "description": "该命令用于移除设计中先前已定义的时钟声明。可以指定移除特定的时钟列表，或者使用 -all 选项移除设计中的所有时钟。",
  "usage": "reset_clock {-all | <clock_list>}",
  "options": [
    {
      "name": "-all",
      "description": "移除设计中的所有时钟。",
      "required": false,
      "type": "flag"
    },
    {
      "name": "<clock_list>",
      "description": "指定需要移除时钟声明的时钟列表。",
      "required": false,
      "type": "clock+"
    }
  ]
}
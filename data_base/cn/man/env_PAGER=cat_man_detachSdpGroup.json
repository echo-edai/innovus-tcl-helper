{
  "command": "detachSdpGroup",
  "is_cmd": true,
  "summary": "将指定的结构化数据路径（SDP）对象从其父组中分离",
  "description": "此命令用于将指定的结构化数据路径（SDP）对象从其父组中分离出来。",
  "usage": "detachSdpGroup [-help] -name {<objNames>}",
  "options": [
    {
      "name": "-help",
      "description": "打印包含每个参数类型和默认信息的简要描述。",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-name",
      "description": "指定要从父组中分离的SDP对象的名称。",
      "required": true,
      "type": "string"
    }
  ]
}
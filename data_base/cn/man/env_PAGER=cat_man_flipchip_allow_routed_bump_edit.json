{
  "command": "flipchip_allow_routed_bump_edit",
  "is_cmd": false,
  "summary": "指定工具在允许操作前是否检查凸点布线状态",
  "description": "此变量控制工具在允许对凸点进行操作（如移动、删除、重新分配、交换）之前，是否检查其布线状态。默认值为1，表示工具忽略布线状态，允许操作已布线的凸点。若设置为0，则工具会检查凸点的布线状态，并禁止对已布线的凸点进行操作。此变量直接影响dbSetFPlanBox命令，当设置为1时，已布线的凸点可被设置到新的位置（通过更改对象边界框实现移动）。所有内部调用dbSetFPlanBox的命令也受此变量影响。",
  "usage": null,
  "options": null
}
Product Version     25.10    Cadence Design Systems, Inc.
addSplitPowerVia(25.10)                                addSplitPowerVia(25.10)

Name
       addSplitPowerVia - Adds or deletes Jenga vias and does not modify vias

Syntax
       addSplitPowerVia

        [-help]

        [-delete | -wires_only]

        [-edgeLength {layer1 value1 layer2 value2 ...}]

        [-forceCount]

        [-localCheckerBoard]

       [-master <cell_name>]

        [-maxViaSize {x y}]

        [-maxWireLength {layer1 value1 layer2 value2 ...}]

        [-maxWireSpacing {layer1 value1 layer2 value2 ...}]

        [-mergeCount {x y}]

        [-mergeDistance {x y}]

        [-minEnclosure]

        [-minViaSize {x y}]

        [-minWireLength {layer1 value1 layer2 value2 ...}]

        [-minWireSpacing {layer1 value1 layer2 value2 ...}]

        [-pinLayer {layer1 layer2 ...}]

        [-prerouteAlign {none samelayer alllayer}]

       [-snap_wire_center_to_grid  {layer1 {none|grid|half_grid|either} layer2
       {none|grid|half_grid|either}} ...]

        [-stackedCheckerBoard]

        [-uda <userDefinedAttribute>]

        [-wireCount {layer1 value1|auto layer2 value2|auto ... }]

       [-wireOffset {layer1 value1 layer2 value2 ...}]

        [-wireWidth {layer1 value1 layer2 value2 ...}]

       Adds or deletes Jenga vias and does not modify vias. It passes  on  all
       Jenga parameters to separate options.

Parameters
       -help  Outputs  a  brief description that includes the type and default
              information for each addSplitPowerVia parameter.

              For a detailed description of the command and  all  its  parame-
              ters, use the man command:

               man addSplitPowerVia.

       -delete
              Deletes  the  via  for selected wires/objects. This is a boolean
              value and is optional.

       -edgeLength {<layer1 value1 layer2 value2 ...>}
              Sets extension length of the wires for each  layer  in  um.  The
              value is a string and is optional.

       -forceCount
              Forces  the  tool  to  generate  the given wirecount even if the
              wires extend beyond the overlap area. This is  a  boolean  value
              and is optional.

       -localCheckerBoard
              Allows local checkerboard cut generation instead of matching the
              global checkerboard pattern.

       -master <cell_name>
              Passes the selected wires through the specified cells.

       -maxViaSize {<x y>}
              Displays  the  maximum  via size. This value is in points and is
              optional.

       -maxWireLength {<layer1 value1 layer2 value2 ...>}
              Sets the maximum length of the wires for each layer in um.  This
              value is a string and is optional.

       -maxWireSpacing {<layer1 value1 layer2 value2 ...>}
              Sets the maximum wire spacing of wires for each layer in um. The
              value is in strings and is optional.

       -mergeCount {<x y>}
              Merges  the  neighboring wire count. The value is an integer and
              is optional.

       -mergeDistance {<x y>}
              Merges the distance in x and y direction. The value is in points
              and is optional.

       -minEnclosure
              Creates the shortest wires that meet DRC rules rather  than  the
              default  wire  length  of  the  entire  overlap  area. This is a
              boolean value and is optional.

       -minViaSize {<x y>}
              Sets the minimum via size. The value is  in  point  and  is  op-
              tional.

       -minWireSpacing {<layer1 value1 layer2 value2 ...>}
              Sets the minimum wire spacing of wires for each layer in um. The
              value is a string and is optional.

       -minWireLength {<layer1 value1 layer2 value2 ...>}
              Sets  the  minimum  length of the wires for each layer in um.The
              value is a string and is optional.

       -snap_wire_center_to_grid {layer1 {none|grid|half_grid|either} layer2
       {none|grid|half_grid|either}}
              Specifies the wire center alignment style for middle layer jenga
              wires. If you do not specify this  parameter,  stripes  are  not
              snapped  to  the  routing grid. Specify one of the following op-
              tions:

              * none: Does no snapping of the wires.

              * grid: Snaps the center of the wire to the routing grid of  the
                same layer as the wire.

              * half_grid:  Snaps  the  center of the wire to the half-routing
                grid. The half-routing grid is a virtual grid with the  tracks
                at  the  center  of  every  two adjacent tracks of the routing
                grid.

              * either: Snaps the center of the wire  to  the  closer  of  the
                routing grid or half-grid track.

       -wireCount {<layer1 value1|auto layer2 value2|auto ...> }
              Sets  the count of wires for each layer in integer or 'auto'. If
              the value of a layer is not set, the tool takes `2' as  default.
              If  -wireCount {Mx auto} is specified, the wire count on Mx will
              be max wire count calculated by intersection  area,  wire  width
              and  wire  spacing. The `auto' setting is mostly used for adding
              split power vias between paralleled wires. The value is a string
              and is optional.

       -wireOffset {<layer1 value1 layer2 value2 ...>}
              Sets the wire offset from the intersection edge of the  selected
              wires  in floating value. It also defines the left edge for ver-
              tical and bottom edges for horizontal middle layer split  wires.
              The value is a string and is optional.

       -wireWidth {<layer1 value1 layer2 value2 ...>}
              Sets the width of the wires for each layer in um. The value is a
              string and is optional.

       -pinLayer {<layer1 layer2>...}
              Lists  pin  layers in a macros used to create via with crossover
              wire.

       -prerouteAlign {<none>|<samelayer>|<alllayer>}
              Defines the alignment of via wires with preexisting wires on the
              same net. The value is numerical and is optional.

       -stackedCheckerBoard
              Creates stacked checker board vias between intermediate layers.

       -uda   Adds the user-defined attribute to the  to-be-built  split  vias
              and wires.

       -wires_only
              Generates only those wires, which are without violations. By de-
              fault,  the  command generates both wires and vias. Prior to im-
              plementing this option, if any via was not  dropped  because  of
              violation, the tool was not generating any wires.

Examples
       Note:  The  command will add split power via after the user selects the
       wires which need to add the split power via (else, no split  power  via
       will be added).

              * The command will add split power via and merge wires which are
                within 1.2 for both x and y direction

                addSplitPowerVia -mergeDistance 1.2

              * The command will add split power via and merge wires which are
                within 1.2 for x direction and 1.5 for y direction.

                addSplitPowerVia -mergeDistance { 1.2 1.5 }

              * The  command will add split power via and merge wires of count
                2 in both x and y direction.

                addSplitPowerVia -mergeCount 2

              * The command will add split power via and merge wires of  count
                2 in x direction and count of 4 in y direction.

                addSplitPowerVia -mergeCount { 2 4}

              * The  command  will add split power via and create wire lengths
                to a max of 1.3 in both x and y direction for all intermediate
                layers within a crossover b/w wire and a wire or b/w wire  and
                a pin.

                addSplitPowerVia -maxWireLength 1.3

Related Information
              * editPowerVia

              * Power Planning and Routing chapter in the Innovus User Guide.

                           Fri Apr 04 01:25:37 2025    addSplitPowerVia(25.10)
Product Version     25.10    Cadence Design Systems, Inc.
write_macro_place_constraint(25.10)        write_macro_place_constraint(25.10)

Name
       write_macro_place_constraint  - Enables you to write constraints into a
       TCL format file from the database

Syntax
        write_macro_place_constraint

       [-help]

       [-cpg_scope {selected macro_only all}]

       -out_file <string>

       [-sections {array basic cpg pg_model_over_macros pg_resource_model}]

       Enables you to write constraints into a TCL format file from the  data-
       base.

       Note:  For 3D designs in Integrity 3D-IC platform, each die carries its
       own macro place constraint information. You must switch to  a  specific
       die before invoking the write_macro_place_constraint command.

Parameters
       -cpg_scope {selected macro_only all}
              Specifies  the instances that are to be written in the CPG guide
              section.

              Data_type: enum, optional

       -help  Outputs a brief description that includes the type  and  default
              information for each write_macro_place_constraint parameter. For
              a detailed description of the command and all of its parameters,
              use the man command:

              man write_macro_place_constraint

       -out_file <string>
              Specifies  the  name  of  the file to which the macro place con-
              straints are written.

              Data_type: string, optional

       -sections {array basic cpg pg_model_over_macros pg_resource_model
       net_length net_resistance}
              Specifies the constraint sections to be written.

              * array: Writes macro array constraints.

              * basic: Writes non-default macro place constraints set  by  the
                set_macro_place_constraint command.

              * cpg:  Writes  out the guide file for initializing the orienta-
                tion of the instances and placing them close to the  specified
                location.

              * pg_model_over_macros: Writes PG modeling for macros.

              * pg_resource_model:  Writes  PG  resource modeling for standard
                cell areas.

                Note: This option is used to model PG over the whole design if
                the -pg_model_over_macros option is not specified.

              Note: Only the basic constraints are dumped out if the -sections
              option is not specified.

              Data_type: enum_list, optional

Example
              * The  following command  writes  the  basic  macro  place  con-
                straints to the xyz file:

                write_macro_place_constraint -cpg_score all -out_file xyz

Related Information
              * check_macro_place_constraint

              * create_pg_model_for_macro_place

              * get_macro_place_constraint

              * set_macro_place_constraint

              * reset_macro_place_constraint

              * Floorplanning the Design chapter in the Innovus User Guide

              * Using the Mixed Placer chapter in the Innovus User Guide

                           Fri Apr 04 01:25:w3r7it2e0_2m5acro_place_constraint(25.10)
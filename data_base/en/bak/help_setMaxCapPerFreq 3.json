{
  "command": "setMaxCapPerFreq",
  "is_cmd": true,
  "summary": "Sets max cap per freq.",
  "description": "Sets max cap per freq.",
  "usage": "setMaxCapPerFreq [-help] -cap {cap1 cap2 ...} [-force] -freq {freq1 freq2 ...} [-lib <libName>] -pins {cell pin}",
  "options": [
    {
      "name": "-help",
      "description": "Prints out the command usage",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-cap",
      "description": "specify a list of Nfreq capacitance values",
      "required": true,
      "type": "flag"
    },
    {
      "name": "-force",
      "description": "Compute maxCap from interpolated -freq and -cap values",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-freq",
      "description": "specify a list of at least two frequency values",
      "required": true,
      "type": "flag"
    },
    {
      "name": "-lib",
      "description": "specify library name",
      "required": false,
      "type": "string"
    },
    {
      "name": "-pins",
      "description": "Add new capacitance constraints to the specified set of cells and pins",
      "required": true,
      "type": "flag"
    }
  ]
}
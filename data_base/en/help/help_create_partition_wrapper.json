{
  "command": "create_partition_wrapper",
  "is_cmd": true,
  "summary": "Creates partition wrapper.",
  "description": "Creates partition wrapper.",
  "usage": "create_partition_wrapper [-help] -partitions <partition_names> [-pushdown] [-wrapper_name <wrapper_suffix_string>]",
  "options": [
    {
      "name": "-help",
      "description": "Prints out the command usage",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-partitions",
      "description": "Specifies the name of one or more partition on which wrapper need to be created",
      "required": true,
      "type": "string"
    },
    {
      "name": "-pushdown",
      "description": "Keep the partition hinst intact for wrapper partition",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-wrapper_name",
      "description": "Specifies the suffix to be added to wrapper partition name. Default is ptnName_wrapper",
      "required": false,
      "type": "string"
    }
  ]
}
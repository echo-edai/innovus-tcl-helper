{
  "command": "unsetMessageLimit",
  "is_cmd": true,
  "summary": "Unsets message limit.",
  "description": "Unsets message limit.",
  "usage": "unsetMessageLimit [-help] [<prefix [ID1, ID2, ...]>]",
  "options": [
    {
      "name": "-help",
      "description": "Prints out the command usage <prefix [ID1, ID2, ...]> # If no prefix, remove the default or user-specified limit on error and warning messages. All errors and warnings are output to log. If has prefix, remove the default or user-specified limit on error and warning message with prefix.",
      "required": false,
      "type": "string"
    }
  ]
}
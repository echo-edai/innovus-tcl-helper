{
  "command": "gui_verbose",
  "is_cmd": false,
  "summary": "Controls which GUI actions are logged. The choices are: actions; but not display-only actions like gui_zoom, gui_pan; are logged, so the .log and .cmd file are incomplete",
  "description": "Controls which GUI actions are logged. The choices are: actions; but not display-only actions like gui_zoom, gui_pan; are logged, so the .log and .cmd file are incomplete",
  "usage": null,
  "options": null
}
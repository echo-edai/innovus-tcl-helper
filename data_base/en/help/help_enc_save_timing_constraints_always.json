{
  "command": "enc_save_timing_constraints_always",
  "is_cmd": false,
  "summary": "Specifies whether SDC files are always copied to the DB directory rather than links to external SDC files. By default they are only copied if the constraints were modified.",
  "description": "Specifies whether SDC files are always copied to the DB directory rather than links to external SDC files. By default they are only copied if the constraints were modified.",
  "usage": null,
  "options": null
}
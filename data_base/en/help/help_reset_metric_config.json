{
  "command": "reset_metric_config",
  "is_cmd": true,
  "summary": "Resets metric config.",
  "description": "Resets metric config.",
  "usage": "reset_metric_config [-help] [-format {html excel text}]",
  "options": [
    {
      "name": "-help",
      "description": "Prints out the command usage",
      "required": false,
      "type": "flag"
    },
    {
      "name": "-format",
      "description": "config for specified report_metric format",
      "required": false,
      "type": "enum"
    }
  ]
}
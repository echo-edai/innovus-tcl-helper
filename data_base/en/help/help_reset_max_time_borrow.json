{
  "command": "reset_max_time_borrow",
  "is_cmd": true,
  "summary": "Resets max time borrow.",
  "description": "Resets max time borrow.",
  "usage": "reset_max_time_borrow [-help] <object_list>",
  "options": [
    {
      "name": "-help",
      "description": "Prints out the command usage",
      "required": false,
      "type": "flag"
    },
    {
      "name": "<object_list>",
      "description": "List of objects: pins, instances or clocks",
      "required": true,
      "type": "string"
    }
  ]
}
{
  "command": "set_max_time_borrow",
  "is_cmd": true,
  "summary": "Sets max time borrow.",
  "description": "Sets max time borrow.",
  "usage": "set_max_time_borrow [-help] <borrow_value> <object_list> [-interface]",
  "options": [
    {
      "name": "-help",
      "description": "Prints out the command usage <borrow_value> # Max time borrow value (float, required) <object_list> # List of objects: pins, instances or clocks",
      "required": true,
      "type": "string"
    },
    {
      "name": "-interface",
      "description": "Specifies for IO paths",
      "required": false,
      "type": "flag"
    }
  ]
}